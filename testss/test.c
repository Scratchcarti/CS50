#include <stdio.h>
#include <stdlib.h>

int main (void)
{
     FILE *input = fopen(input.wav, "r");

     FILE *output = fopen(output3.wav, "w");

     fclose(input);
    fclose(output);
}














